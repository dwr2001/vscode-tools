{
  "name": "vscode-tools",
  "displayName": "vscode-tools",
  "description": "A VS Code extension with useful tools",
  "version": "0.0.1",
  "publisher": "dwr2001",
  "repository": {
    "type": "git",
    "url": "https://github.com/dwr2001/vscode-tools.git"
  },
  "engines": {
    "node": ">=18",
    "vscode": "^1.82.0"
  },
  "categories": ["Other"],
  "activationEvents": [],
  "main": "./dist/extension.js",
  "files": ["LICENSE", "package.json", "dist"],
  "contributes": {
    "commands": [
      {
        "command": "vscode-tools.helloWorld",
        "title": "Hello World",
        "category": "VSCode Tools"
      },
      {
        "command": "vscode-tools.createNewFile",
        "title": "Create New File",
        "category": "VSCode Tools"
      },
      {
        "command": "vscode-tools.readFile",
        "title": "Read File",
        "category": "VSCode Tools"
      }
    ],
    "views": {
      "explorer": [
        {
          "id": "vscode-tools.view",
          "name": "VSCode Tools View",
          "icon": "$(tools)",
          "type": "webview"
        }
      ]
    },
    "configuration": {
      "title": "VSCode Tools",
      "properties": {
        "vscode-tools.deepseek.apiKey": {
          "type": "string",
          "default": "",
          "description": "DeepSeek API Key",
          "markdownDescription": "您的DeepSeek API Key。可以在 [DeepSeek平台](https://platform.deepseek.com/api_keys) 获取。"
        },
        "vscode-tools.deepseek.baseURL": {
          "type": "string",
          "default": "https://api.deepseek.com/v1",
          "description": "DeepSeek API Base URL",
          "markdownDescription": "DeepSeek API的基础URL。通常不需要修改。"
        },
        "vscode-tools.deepseek.model": {
          "type": "string",
          "default": "deepseek-chat",
          "description": "DeepSeek Model",
          "enum": ["deepseek-chat", "deepseek-coder"],
          "markdownDescription": "要使用的DeepSeek模型。`deepseek-chat` 适用于一般对话，`deepseek-coder` 适用于编程任务。"
        }
      }
    }
  },
  "scripts": {
    "build": "rsbuild build",
    "dev": "rsbuild build --watch",
    "package": "vsce package --no-dependencies && mv vscode-tools-*.vsix ../..",
    "format": "biome format --write"
  },
  "dependencies": {
    "@vscode-tools/protocol": "*",
    "uri-js": "^4.4.1"
  },
  "devDependencies": {
    "@vscode/vsce": "^3.6.0",
    "@biomejs/biome": "^1.9.0"
  }
}
